/**
 * Value used as 'functionName' when we have a member expression with the name as 'catch'.
 *
 * @default
 * @type {string}
 */
const MEMBER_EXPRESSION_CATCH = 'memberExpressionCatch';

/**
 * Value used for the logger, by default if it is not specified will use `console`
 *
 * @default
 * @type {string}
 */
const LOGGER_API = 'console';

export default {
  LOGGER_API,
  MEMBER_EXPRESSION_CATCH,
};
